## 1. API Wire Integration

- [x] 1.1 Add `ResponseFormat` deserialization in `engine-server/src/models.rs`.
- [x] 1.2 Wire `Grammar` state machine initialization into `handle_chat_completion`.

## 2. Validation & Tool-Calling E2E Test

- [x] 2.1 Add automated test verifying valid JSON output under complex nested schemas.
