# OS
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Rust / Cargo
engine/target/

# downloads / fixtures / models
testdata/*.gguf
testdata/.cache/
models/
*.gguf
llama.cpp/

# local benchmark scratch
t*.json
texto.json
val.json

# nsys traces (large binaries, regenerated on demand)
engine/f2_trace.nsys-rep
engine/f2_trace.sqlite
engine/f2_trace_*.csv

# local working artifacts (bot session plans, traces, scratch) — never pushed
local-artifacts/
